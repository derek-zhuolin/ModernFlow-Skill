# Build artefacts — regenerated by scripts/, and subsetted to whatever page
# they were last run against, so committing them would be misleading.
out/
assets/fonts/*.woff2
assets/fonts/fonts.css

*.built.html
__pycache__/
.DS_Store
